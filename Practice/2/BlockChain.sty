% IntroductiontoAI.sty - Style file for Introduction to AI experiment reports

%% ============================================================================
%% ==                         Loading Packages                             ==
%% ============================================================================

% --- Basic Functionality ---
\usepackage{blindtext}    % For generating sample text (useful for testing layout)
\usepackage{ctex}         % Provides comprehensive Chinese language support
\usepackage{indentfirst}  % Indents the first paragraph after a section heading

% --- Tables ---
\usepackage{booktabs}     % For creating professional-quality tables

% --- Mathematics ---
\usepackage{amsmath}      % Enhanced support for mathematical formulas
\usepackage{anyfontsize}  % Addresses potential font size issues in formulas

% --- Figures and Captions ---
\usepackage{caption}      % Allows customization of captions for figures and tables
\usepackage{subcaption}   % Enables the creation of subfigures and subtables

% --- Cross-referencing ---
\usepackage{cleveref}     % Provides intelligent cross-referencing (e.g., automatically uses "Figure" or "Figures")

% --- Code Listings (Handled by listings package loaded in .cls) ---
% Note: The listings package itself is loaded in the associated .cls file.
% This .sty file configures its behavior.

%% ============================================================================
%% ==                      List Spacing Configuration                      ==
%% ============================================================================

% --- Compact list spacing (Optional) ---
% Uncomment the following lines if more compact lists (itemize, enumerate, description) are desired.
% \usepackage{paralist}
% \let\itemize\compactitem
% \let\enditemize\endcompactitem
% \let\enumerate\compactenum
% \let\endenumerate\endcompactenum
% \let\description\compactdesc
% \let\enddescription\endcompactdesc

%% ============================================================================
%% ==                   Code Listing Configuration (listings)              ==
%% ============================================================================

% --- General Listings Settings ---
\lstset{
    basicstyle          =   \sffamily,          % Base font style for code (sans-serif)
    keywordstyle        =   \bfseries,          % Style for keywords (bold)
    commentstyle        =   \rmfamily\itshape,  % Style for comments (roman, italic)
    stringstyle         =   \ttfamily,          % Style for strings (monospace)
    flexiblecolumns,                            % Allow flexible column widths
    numbers             =   left,               % Display line numbers on the left
    showspaces          =   false,              % Do not visualize spaces
    numberstyle         =   \zihao{-5}\ttfamily, % Style for line numbers (small size, monospace)
    showstringspaces    =   false,              % Do not visualize spaces within strings
    captionpos          =   t,                  % Position captions above the code block
    frame               =   shadowbox,          % Draw a shadowed box frame around the code
    rulesepcolor        =   \color{red!20!green!20!blue!20}, % Color for frame rules (light gray)
    breaklines          =   true,               % Automatically break long lines
    breakatwhitespace   =   true,               % Prefer breaking lines at whitespace
    % Add other general settings as needed
}

% --- Language Definitions ---

% Define JavaScript language specifics
\lstdefinelanguage{JavaScript}{
  keywords={break, case, catch, continue, debugger, default, delete, do, else, finally, for, function, if, in, instanceof, new, return, switch, this, throw, try, typeof, var, void, while, with, let, const, class, export, import, super, extends}, % List of keywords
  morecomment=[l]{//},             % Single-line comments start with //
  morecomment=[s]{/*}{*/},         % Multi-line comments delimited by /* */
  morestring=[b]',                % Strings delimited by single quotes
  morestring=[b]",                % Strings delimited by double quotes
  morestring=[b]`,                % Template literals delimited by backticks
  sensitive=true                  % Language is case-sensitive
}

% Define HTML language specifics
\lstdefinelanguage{HTML}{
  keywords={a, abbr, address, area, article, aside, audio, b, base, bdi, bdo, blockquote, body, br, button, canvas, caption, cite, code, col, colgroup, data, datalist, dd, del, details, dfn, dialog, div, dl, dt, em, embed, fieldset, figcaption, figure, footer, form, h1, h2, h3, h4, h5, h6, head, header, hr, html, i, iframe, img, input, ins, kbd, label, legend, li, link, main, map, mark, meta, meter, nav, noscript, object, ol, optgroup, option, output, p, param, picture, pre, progress, q, rp, rt, ruby, s, samp, script, section, select, small, source, span, strong, style, sub, summary, sup, svg, table, tbody, td, template, textarea, tfoot, th, thead, time, title, tr, track, u, ul, var, video, wbr}, % List of HTML tags
  sensitive=false,                % HTML tags are case-insensitive (though lowercase is standard)
  morecomment=[s]{<!--}{-->},      % HTML comments delimited by <!-- -->
  morestring=[b]",                % Attribute values in double quotes
  morestring=[b]'                 % Attribute values in single quotes
}

% Define CSS language specifics
\lstdefinelanguage{CSS}{
  keywords={align-content, align-items, align-self, all, animation, animation-delay, animation-direction, animation-duration, animation-fill-mode, animation-iteration-count, animation-name, animation-play-state, animation-timing-function, backface-visibility, background, background-attachment, background-blend-mode, background-clip, background-color, background-image, background-origin, background-position, background-repeat, background-size, border, border-bottom, border-bottom-color, border-bottom-left-radius, border-bottom-right-radius, border-bottom-style, border-bottom-width, border-collapse, border-color, border-image, border-image-outset, border-image-repeat, border-image-slice, border-image-source, border-image-width, border-left, border-left-color, border-left-style, border-left-width, border-radius, border-right, border-right-color, border-right-style, border-right-width, border-spacing, border-style, border-top, border-top-color, border-top-left-radius, border-top-right-radius, border-top-style, border-top-width, border-width, bottom, box-decoration-break, box-shadow, box-sizing, break-after, break-before, break-inside, caption-side, caret-color, clear, clip, color, column-count, column-fill, column-gap, column-rule, column-rule-color, column-rule-style, column-rule-width, column-span, column-width, columns, content, counter-increment, counter-reset, cursor, direction, display, empty-cells, filter, flex, flex-basis, flex-direction, flex-flow, flex-grow, flex-shrink, flex-wrap, float, font, font-family, font-feature-settings, font-kerning, font-language-override, font-size, font-size-adjust, font-stretch, font-style, font-synthesis, font-variant, font-variant-caps, font-variant-east-asian, font-variant-ligatures, font-variant-numeric, font-variant-position, font-weight, grid, grid-area, grid-auto-columns, grid-auto-flow, grid-auto-rows, grid-column, grid-column-end, grid-column-gap, grid-column-start, grid-gap, grid-row, grid-row-end, grid-row-gap, grid-row-start, grid-template, grid-template-areas, grid-template-columns, grid-template-rows, hanging-punctuation, height, hyphens, image-rendering, isolation, justify-content, left, letter-spacing, line-break, line-height, list-style, list-style-image, list-style-position, list-style-type, margin, margin-bottom, margin-left, margin-right, margin-top, max-height, max-width, min-height, min-width, mix-blend-mode, object-fit, object-position, opacity, order, orphans, outline, outline-color, outline-offset, outline-style, outline-width, overflow, overflow-wrap, overflow-x, overflow-y, padding, padding-bottom, padding-left, padding-right, padding-top, page-break-after, page-break-before, page-break-inside, perspective, perspective-origin, pointer-events, position, quotes, resize, right, scroll-behavior, tab-size, table-layout, text-align, text-align-last, text-combine-upright, text-decoration, text-decoration-color, text-decoration-line, text-decoration-style, text-indent, text-justify, text-orientation, text-overflow, text-shadow, text-transform, text-underline-position, top, transform, transform-origin, transform-style, transition, transition-delay, transition-duration, transition-property, transition-timing-function, unicode-bidi, user-select, vertical-align, visibility, white-space, widows, width, word-break, word-spacing, word-wrap, writing-mode, z-index}, % Extensive list of CSS properties
  sensitive=false,                % CSS properties are case-insensitive
  morecomment=[s]{/*}{*/},         % CSS comments delimited by /* */
  morestring=[b]",                % Strings in double quotes
  morestring=[b]'                 % Strings in single quotes
}

\lstdefinelanguage{Python}{
  keywords={and, as, assert, break, class, continue, def, del, elif, else, except, False, finally, for, from, global, if, import, in, is, lambda, None, nonlocal, not, or, pass, raise, return, True, try, while, with, yield}, % List of Python keywords
  morecomment=[l]{\#},            % Single-line comments start with #
  morestring=[b]',                % Strings delimited by single quotes
  morestring=[b]",                % Strings delimited by double quotes
  sensitive=true                 % Language is case-sensitive
}

% --- Language Styles ---

% Define a style for JavaScript code blocks
\lstdefinestyle{JavaScript}{
    language=JavaScript,            % Use the JavaScript language definition
    keywordstyle=\color{blue},       % Color keywords blue
    commentstyle=\color{red!60!black}, % Color comments dark red
    stringstyle=\color{green!60!black},  % Color strings dark green
    basicstyle=\ttfamily\small,    % Use small monospace font
    showstringspaces=false,         % Do not visualize spaces in strings
    breaklines=true,                % Allow line breaks
    % Add other JavaScript-specific style settings if needed
}

% Define a style for HTML code blocks
\lstdefinestyle{html}{
    language=HTML,                  % Use the HTML language definition
    keywordstyle=\color{blue},       % Color tags blue
    commentstyle=\color{red!60!black}, % Color comments dark red
    stringstyle=\color{green!60!black},  % Color attribute values dark green
    basicstyle=\ttfamily\small,    % Use small monospace font
    showstringspaces=false,         % Do not visualize spaces in strings
    breaklines=true,                % Allow line breaks
    % Add other HTML-specific style settings if needed
}

% Define a style for CSS code blocks
\lstdefinestyle{css}{
    language=CSS,                   % Use the CSS language definition
    keywordstyle=\color{blue},       % Color properties blue
    commentstyle=\color{red!60!black}, % Color comments dark red
    stringstyle=\color{green!60!black},  % Color values dark green
    basicstyle=\ttfamily\small,    % Use small monospace font
    showstringspaces=false,         % Do not visualize spaces in strings
    breaklines=true,                % Allow line breaks
    % Add other CSS-specific style settings if needed
}

\lstdefinestyle{Python}{
    language=Python,                % Use the Python language definition
    keywordstyle=\color{blue},       % Color keywords blue
    commentstyle=\color{red!60!black}, % Color comments dark red
    stringstyle=\color{green!60!black},  % Color strings dark green
    basicstyle=\ttfamily\small,    % Use small monospace font
    showstringspaces=false,         % Do not visualize spaces in strings
    breaklines=true,                % Allow line breaks
    % Add other Python-specific style settings if needed
}

%% ============================================================================
%% ==        Figure, Caption, and Referencing Configuration              ==
%% ============================================================================

% --- Figure/Subfigure Caption Customization ---
\renewcommand{\figurename}{Figure} % Change the default name "Figure" (if needed, e.g., for language)
\DeclareCaptionFormat{subfig}{Figure \thefigure~-~(#1): #3} % Custom format for subfigure captions: "Figure <main_fig_num> - (<subfig_letter>): <subfig_caption>"
\captionsetup[subfigure]{format=subfig, labelformat=simple} % Apply the custom format to subfigures
\renewcommand{\thesubfigure}{\alph{subfigure}} % Use lowercase letters (a, b, c) for subfigure numbering

% --- Subfigure Reference Format Customization ---
% Modifies how \ref{...} refers to subfigures
\makeatletter
\renewcommand{\p@subfigure}{\thefigure~-~(} % Prefix for subfigure references: "<main_fig_num> - ("
\renewcommand{\thesubfigure}{\alph{subfigure})} % Subfigure number format within references: "a)", "b)", etc.
\makeatother

% --- Cross-reference Naming (cleveref) ---
\crefname{figure}{Figure}{Figures} % Define singular and plural names for figure references used by \cref
\crefname{subfigure}{Figure}{Figures} % Define singular and plural names for subfigure references used by \cref (often same as figure)

%% ============================================================================
%% ==                         Layout Adjustments                           ==
%% ============================================================================

\raggedbottom     % Allows the bottom margin to be flexible, preventing excessive stretching of vertical space on pages that are not full.
\setlength{\parskip}{0.5\baselineskip} % Sets the space between paragraphs to half a line height.

%% ============================================================================
%% ==                         End of Style File                            ==
%% ============================================================================
